#include <iostream>
#include <sstream>
#include <string>
using namespace std;

int main() {
	int N, Q;
	cin >> N >> Q;

	stringstream ss;
	//for (int i = 0; i < N; i++) {
		string query;
		getline(cin, query);
		//query.substr(1, query.length() - 2);
		cout << query << endl;

	//}

	//for (int i = 0; i < Q; i++) {

	//}

	return 0;
}